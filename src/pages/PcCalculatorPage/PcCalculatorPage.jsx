import React from "react";
import { connect } from "react-redux";

export const PcCalculatorPage = (props) => {
  return <div>PcCalculatorPage</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(PcCalculatorPage);
