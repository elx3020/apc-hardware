import React from "react";
import { connect } from "react-redux";

export const ProductResultPage = (props) => {
  return <div>ProductResultPage</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(ProductResultPage);
